{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "verbosity": {
            "title": "Verbosity",
            "description": "This optional parameter overrides the verbosity level of the collector.",
            "type": "string",
            "enum": ["none", "error", "warning", "info", "debug"]
        }
    }
}
